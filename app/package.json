{
  "name": "zrefdocphoto",
  "version": "1.0.0",
  "private": true,
  "description": "Fotos zu Referenzbelegen (Transport / Auslieferung / Handling Unit) - Fiori Elements App auf Basis von RAP",
  "scripts": {
    "start": "fiori run --open index.html",
    "start-local": "fiori run --config ui5-local.yaml --open index.html"
  },
  "devDependencies": {
    "@sap/ux-ui5-tooling": "^1",
    "@ui5/cli": "^3"
  }
}
